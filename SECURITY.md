# 安全策略

## 支持范围

安全修复优先落在默认分支 `main`。当前尚未发布带长期支持承诺的版本；使用旧提交自行构建的部署，请先升级到 `main` 的最新可用提交后再复测。

## 报告漏洞

请**不要**通过公开 Issue、讨论区或日志粘贴漏洞细节、Provider 凭据、Bearer token、私有镜像名称或已签名下载链接。

请在本仓库的 **Security** 标签页选择 **Report a vulnerability**，提交私密漏洞报告。报告应尽量包含：

- 受影响的 DRG 提交或构建版本、操作系统和 Docker Engine/Desktop 版本；
- 可复现步骤、预期行为与实际行为；
- 最小化的脱敏配置片段和日志；
- 影响范围，以及是否可能暴露上游凭据、绕过 TLS/完整性校验或造成服务中断。

如果私密报告入口暂不可用，请先创建不含技术细节的公开 Issue，请求维护者提供私密联系渠道；不要在该 Issue 中披露 PoC 或敏感材料。

## 处理原则

维护者会确认收到私密报告、评估影响，并在修复和发布协调完成前保持细节非公开。修复过程中可能会要求提供额外的脱敏复现信息；请勿发送真实密码、PAT、Docker 配置目录中的控制令牌或完整诊断历史。

确认修复后，维护者会在发布说明中描述受影响范围、缓解方式和升级步骤，并在不增加使用者风险的前提下致谢报告者。

## 本项目特别关注的风险

DRG 位于 Docker daemon 与上游 Registry 之间。下列问题应优先报告：

- Provider 凭据、短期 token、Authorization 头、Cookie 或签名 URL 泄漏到日志、下游客户端或其他 Provider；
- 本地 CA、外部 TLS 或证书信任处理导致的错误信任、主机名校验绕过或不安全降级；
- manifest/blob digest 校验、Range 续传或跨 Provider 切换导致的内容完整性问题；
- 非预期的公开监听、路径遍历、本地控制面未授权访问或资源耗尽；
- 依赖项中的已知高危漏洞，且其利用路径能影响 DRG 运行时。
